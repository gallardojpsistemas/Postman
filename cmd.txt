comandos (ejecutar terminal como administrador)

#newman run ReqRes.postman_collection.json

#npm install -g newman-reporter-htmlextra

#newman run ReqRes.postman_collection.json -r htmlextra

#newman run ReqRes.postman_collection.json -r htmlextra -e prod.postman_environment.json

Se guarda en una nueva carpeta llamada "newman"